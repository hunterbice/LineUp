> Archived historical document. Not canonical for the native Swift rebuild. See `AGENTS.md` and `docs/native-*` for current direction.

# Priority 16 App Store Readiness Audit

Audit target: LineUp web/PWA at v72 before the iOS wrapper pass.

| Area | Files inspected | Finding | Action taken | Status |
| --- | --- | --- | --- | --- |
| App completeness / placeholder content | `src/ui/*`, `src/main.js`, `index.html`, manifests, smoke scripts | The entry, Live, Deals, detail, Profile, and report flows are functional. Public-copy sweeps found no dead launch wall, fake partner claim, fake Apple button, or public placeholder card. | Added honest fall Early Access positioning and replaced the alcohol-led deal example with review-safe examples. | FIXED |
| Early Access Mode | `renderShell.js`, `renderDashboard.js`, `renderProfile.js`, `earlyAccessController.js`, `earlyAccessService.js`, `early-access/index.ts`, `202606230001_app_store_early_access.sql` | Joining Early Access, confirming University of Arizona, saving venues, viewing venue/deal pages, and requesting aggregate launch-deal interest are functional backend-confirmed actions. | Added authenticated Early Access state and rate-limited launch-deal requests with aggregate-only venue access. Live failures now show unavailable/low-data states instead of seeded crowd truth. | FIXED |
| Account creation and deletion | Supabase Auth flow, `account-sync`, `renderProfile.js`, `main.js`, auth-linked migrations, live security smoke | Email/password account creation works. The signed-device self-delete path deletes the authenticated user and linked personal activity, signs out locally, and cannot target a supplied second user ID. Aggregate, de-identified venue statistics may remain. | Added in-app confirmation and server-side self-delete. Live two-user test proved user A was deleted while user B survived. | FIXED |
| Login methods and Sign in with Apple risk | `renderShell.js`, Supabase auth calls, provider searches, reliability smoke | Student auth is first-party email/password only. No Google/social login and no Apple button are rendered. | Retained email/password for first submission and removed unsupported future Apple-identifier copy. | PASS |
| Location permission and manual fallback | `locationService.js`, presence logic, `renderShell.js`, `renderProfile.js`, area selector | Foreground location is optional. University of Arizona campus selection and area browsing work without permission, and the UI explains aggregate accuracy without claiming background tracking. | Added explicit campus/manual fallback and optional-location language; documented the Priority 17 native purpose string. | FIXED |
| Privacy policy / terms / support links | `renderProfile.js`, `public/legal/privacy.html`, `public/legal/terms.html`, `public/legal/support.html`, PWA smoke | Complete in-app summaries and public light-mode pages are reachable. Support has a mailto path and issue-report path; deletion is not support-dependent. | Added Privacy, Terms, Support, deletion/retention, venue aggregate-sharing, and contact disclosures. | FIXED |
| App Privacy Label data inventory | services, Edge Functions, schema/migrations, owner dashboard, `docs/app-store-privacy-label-inventory.md` | Account, profile, favorites, reports, location/presence, signed-device, analytics, diagnostics, roles, and request data are inventoried conservatively. | Added evidence-linked categories, purposes, linkage/tracking/sharing, and retention notes for App Store Connect. | FIXED |
| Age rating / alcohol-nightlife copy risk | renderers, seed copy, deal editor examples, `docs/app-store-age-rating-notes.md` | The product remains nightlife-oriented, but public/static examples no longer lead with wells/shots or excessive drinking. No gambling, explicit content, or public social feed was found. | Replaced risky examples and documented age-rating answers that must be confirmed in App Store Connect. | FIXED |
| User-generated content risk | report sheet, `location-ingest`, `reports-feed`, detail rendering, reliability/security smokes | iOS v1 reports expose only structured crowd, wait, cover, identity mode, and verification signals. No public note, photo, chat, or free-form feed is rendered. | Removed note entry, note persistence for new reports, and note return/display while preserving structured reporting. | FIXED |
| Payments / In-App Purchase risk | student renderers, venue deal/subscription UI, migrations, public-copy sweep | No Stripe, purchase, upgrade, subscription, or external checkout CTA is present in normal student UI. Authorized venue tools manage existing content only. | Removed the external LineLeap CTA from venue detail; documented B2B sales/payment as outside the iOS app for this submission. | FIXED |
| App Review demo/reviewer access needs | account gate, role gates, app smoke fixtures, `docs/app-review-submission-notes.md` | Normal features require an account; owner/staff controls are not needed for student review. Reviewer steps are documented, but a disposable credential must be created and entered only in App Store Connect during Priority 17. | Added review notes and a no-secrets reviewer-account procedure. | DEFERRED |
| Screenshots/metadata readiness | app shell, Live, Deals, detail, profile, `docs/app-store-metadata-checklist.md` | Required metadata fields and honest screenshot subjects are documented; device captures must come from the Priority 17 submitted build. | Added the submission checklist and prohibited unbuilt/fake screenshot content. | DEFERRED |
| PWA-to-iOS-wrapper readiness notes | manifest, service worker, install prompt, location/auth flows, readiness docs | PWA content and routes are wrapper-ready; native purpose strings, associated domains, navigation allowlists, native device QA, and TestFlight signing remain intentionally out of scope. | Documented exact wrapper follow-up without creating native files. | DEFERRED |
| Offline/no-network behavior | root/public offline pages, service worker, PWA smoke | Light offline fallback and retry action exist. Dynamic backend data is not presented as freshly loaded by the offline page. | Preserve and regression-test the existing fallback. | PASS |
| Security/source-of-truth preservation | Priority 13 ledger, security/source smokes, `early-access`, `account-sync`, direct-write grants, live security smoke | New mutations require authenticated signed-device Edge Functions. Direct request-table writes are revoked; venue interest is aggregate and role-gated; deletion is self-bound. All static and live denial tests passed. | Deployed the migration/functions and extended static plus live IDOR/BOLA coverage. | PASS |

## Reviewer-access follow-up

Before TestFlight/App Review, create one intentionally disposable, non-admin reviewer account in production and enter it only in App Store Connect review notes. Do not commit its password or grant it owner/staff roles.

## Native-wrapper follow-up

Priority 17 must add the iOS location purpose string from `docs/location-permission-review.md`, configure associated domains/navigation allowlists, confirm account deletion in the wrapped WebView, and capture screenshots from the submitted build.

## Verification evidence

- Migration `202606230001_app_store_early_access.sql` is present locally and remotely.
- Edge Functions `early-access`, `account-sync`, `reports-feed`, and `location-ingest` are deployed and active.
- Live security smoke passed with two disposable users, signed devices, direct-write denials, cross-user/cross-venue denials, Early Access join/request, and self-only deletion.
- `npm run smoke:source`, `npm run smoke:reliability`, `npm run smoke:pwa`, `npm run smoke:security`, `npm run build`, `npm run smoke:app`, and `npm test` pass at v73.
- GitHub Pages deployment `28055235732` completed successfully. `https://get-lineup.app/sw.js` serves `lineup-pwa-v73`; both icon cache-busters are `?v=73`; Privacy, Terms, and Support return HTTP 200; and the deployed bundle contains the Early Access, launch-deal request, account deletion, and Deals surfaces.
- Native-wrapper checks remain intentionally pending until Priority 17.
